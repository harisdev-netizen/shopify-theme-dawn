<style>
  .collection-list-container-{{ section.id }} {
    width: 90vw;
    display: grid;
    grid-template-columns: 1fr 1fr 1fr 1fr;
    gap: 1rem;
    margin: auto;
    box-sizing: border-box;
  }

  .collection-image-{{ section.id }} {
    width: 100%;
    height: auto;
    object-fit: cover;
  }
</style>


<div class="collection-list-container-{{ section.id }}">
  <h4>{{ section.settings.collection_list_heading }}</h4>
  {% for block in section.blocks %}
    <a class="collection-link-{{ section.id }}" href="{{ block.settings.collection_link }}">
      <img class="collection-image-{{ section.id }}" src="{{ block.settings.collection_image | image_url }}" />
    </a>
  {% endfor %}
</div>



{% schema %}
  {
    "name": "Collection List New",
    "settings": [
      {
        "type": "text",
        "id": "collection_list_heading",
        "label": "Collection Heading",
        "default": "<h4>Explore our Best products</h4>"
      }
    ],
    "blocks": [
      {
        "type": "collection-list",
        "name": "collection",
        "settings": [
          {
            "type": "image_picker",
            "id": "collection_image",
            "label": "Select Image"
          },
          {
            "type": "url",
            "id": "collection_link",
            "label": "Select Collection"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Collection List New"
      }
    ]
  }
{% endschema %}